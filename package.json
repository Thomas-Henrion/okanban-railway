{
  "name": "s07-okanban-final-blue-benoitoclock",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run dev --prefix ./back & npm run dev --prefix ./front",
    "start": "npm install --prefix ./back && npm install --prefix ./front && npm run build --prefix ./front && npm run start --prefix ./back"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
